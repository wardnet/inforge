{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "service.json",
  "title": "Service resource",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "container", "host", "type", "user"],
  "properties": {
    "name": { "type": "string", "pattern": "^[a-z][a-z0-9_-]*$" },
    "container": { "type": "string", "minLength": 1 },
    "host": { "type": "string", "minLength": 1 },
    "type": { "type": "string", "enum": ["raw", "container"] },
    "user": { "type": "string", "minLength": 1 },
    "secrets": {
      "type": "object",
      "description": "Optional runtime secrets. Each key is the env-var name set in the service process; the value is a source DSL string: ref:<database|compute>/<name>.<output>, vault:<KEY>, env:<VAR>, or a literal string.",
      "additionalProperties": { "type": "string", "minLength": 1 }
    },
    "ingress": {
      "type": "array",
      "description": "typed inbound routes realized on the host's nginx. Each entry binds a public listen port; tls-termination entries may share a port (SNI demux), a forward port is single-service-exclusive.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "listen", "target"],
        "properties": {
          "type": {
            "type": "string",
            "enum": ["tls-termination", "forward"],
            "description": "tls-termination: nginx terminates ACME TLS for the service's SNIs and reverse-proxies to target. forward: nginx stream-forwards the raw L4 connection to target with the PROXY protocol; the backend owns its TLS."
          },
          "listen": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535,
            "description": "public port nginx accepts traffic on (required; no default; must differ from target)"
          },
          "target": {
            "type": "integer",
            "minimum": 1,
            "maximum": 65535,
            "description": "loopback port the service listens on; nginx reverse-proxies/forwards to 127.0.0.1:target (required; must differ from listen). A raw public port with no proxy belongs on the compute's firewall.inbound, not here."
          },
          "vanity": {
            "type": "array",
            "description": "extra public FQDNs (beyond the auto-derived <svc>.svc name) a tls-termination entry serves. A bare token is env+region-scoped; a value with a dot or a {BASE_DOMAIN}/{ENV}/{REGION_SLUG} placeholder is a literal FQDN. Valid only on tls-termination (forward has no SNI).",
            "items": { "type": "string", "minLength": 1 }
          }
        }
      }
    }
  }
}
