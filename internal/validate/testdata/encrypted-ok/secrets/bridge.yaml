name: bridge
container: bridge
provider: infisical
secrets:
  DATABASE_URL:
    source: "ref:database/bridge.connectionUrl"
  SERVER_IP:
    source: "ref:compute/bridge-01.publicIp"
  CF_TOKEN:
    source: "${CLOUDFLARE_API_TOKEN}"
  API_TOKEN:
    source: "encrypted"
