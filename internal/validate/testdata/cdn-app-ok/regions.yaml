regions:
  us-east-1:
    slug: use1
    cdn:
      provider: cloudflare
    providers:
      cloudflare:
        apiToken: test-token
global:
  placementRegion: us-east-1
  cdn:
    provider: cloudflare
  providers:
    cloudflare:
      apiToken: test-token
