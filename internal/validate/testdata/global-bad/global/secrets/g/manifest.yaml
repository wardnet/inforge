# A global secret referencing a REGIONAL database — rejected: the global slice is
# validated in a global-only context, so the regional database is not found.
name: g
container: shared
provider: infisical
secrets:
  DATABASE_URL:
    source: "ref:database/regionaldb.connectionUrl"
