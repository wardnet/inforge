# A regional secret resolving a GLOBAL database output: the one allowed
# cross-region reference. The global/ prefix targets the global slice's database.
name: app
container: app
provider: infisical
secrets:
  DATABASE_URL:
    source: "ref:database/global/shared.connectionUrl"
