# multiple source DSL failures: malformed grammar, dangling database ref, and
# an unknown compute output.
name: bridge
container: bridge
provider: infisical
secrets:
  MALFORMED:
    source: "nonsense"
  MISSING_DB:
    source: "ref:database/ghost.connectionUrl"
  WRONG_OUTPUT:
    source: "ref:compute/bridge-01.privateIp"
