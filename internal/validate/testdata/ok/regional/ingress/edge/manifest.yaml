# The standalone ingress tier (ADR-0026): nginx fronting the bridge services.
name: edge
container: bridge
host: bridge
# Public health port (default 81) — opened only because a service declares its own
# health_probes_port (see web).
health_probes_port: 81
